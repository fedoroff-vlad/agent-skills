<!--
  TEMPLATE: RUN.md  ·  audience: human  ·  language: ONBOARDING_LANG (default ru)
  The distilled HAPPY PATH — only steps actually executed and verified (dead ends
  stayed in journey-log.md). Source: journey-log.md + project-map.md.
  Fill conventions (same as document-project/references/templates/README.md):
    {{TOKEN}} = one value · <!-- REPEAT --> = one block per item ·
    <!-- OPTIONAL --> = keep only if the condition holds ·
    translate prose only — commands, env-key names, ports, paths stay verbatim.
  Redact every secret VALUE. Delete this comment in the output.
-->

The template below is the fixed shape. Emit it as `RUN.md` at the repo root.

```markdown
# Running {{PROJECT_NAME}}

_Verified {{DATE}} on {{OS}} · commit {{SHA}}. Follow top to bottom._

## 0. Prerequisites
<!-- REPEAT one bullet per prerequisite (Docker daemon running, toolchain, cert imported): -->
- {{PREREQUISITE}}
<!-- /REPEAT -->

## 1. Toolchain
```
{{TOOLCHAIN_SELECT_CMD}}
```

## 2. Configuration — `.env`
```
cp .env.example .env
```
| Key | What it is | Secret? | Where to get it |
|---|---|---|---|
<!-- REPEAT one row per env key: -->
| {{KEY}} | {{KEY_PURPOSE}} | {{KEY_SECRET}} | {{KEY_SOURCE}} |
<!-- /REPEAT -->
> Secrets: you will be asked for these — they are not stored in the repo.

## 3. Backing services
<!-- OPTIONAL local via compose: -->
```
{{COMPOSE_UP_CMD}}
```
<!-- /OPTIONAL -->
<!-- OPTIONAL forwarded from a cluster: -->
```
# macOS / Linux
sh docs/onboarding/scripts/port-forward.sh
# Windows
powershell -NoProfile -File docs/onboarding/scripts/port-forward.ps1
```
This forwards: {{FORWARDED_SERVICES}}.
<!-- /OPTIONAL -->

## 4. Build
```
{{BUILD_CMD}}
```

## 5. Run
```
{{RUN_CMD}}
```

## 6. Verify it is up
```
{{HEALTHCHECK_CMD}}    # expect: {{HEALTHCHECK_EXPECT}}
```

## Troubleshooting
| Symptom | Fix |
|---|---|
<!-- REPEAT one row per wall you hit and cleared: -->
| {{SYMPTOM}} | {{FIX}} |
<!-- /REPEAT -->

---
_generated by run-guide from journey-log.md @ {{SHA}}_
```
