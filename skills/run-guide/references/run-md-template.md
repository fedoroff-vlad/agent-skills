# RUN.md template

The distilled **happy path** — only steps you actually executed and verified.
No dead ends (those stayed in `journey-log.md`). A newcomer should get from clone
to a running app by following this top to bottom. Redact every secret value.

```markdown
# Running <project>

_Verified <date> on <OS> · commit <sha>. Follow top to bottom._

## 0. Prerequisites
- <tool> <version>   # e.g. JDK 25 (Temurin), Docker 24+, kubectl, a kube-context
- <corporate cert imported into the JVM truststore>  # if your build needed it

## 1. Toolchain
```
<how to select the right JDK/Node/etc — jenv/sdkman/nvm command>
```

## 2. Configuration — `.env`
Copy the template and fill the values:
```
cp .env.example .env
```
| Key | What it is | Secret? | Where to get it |
|---|---|---|---|
| DB_URL | Postgres JDBC url | no | RUN.md §3 |
| DB_PASSWORD | Postgres password | **yes** | ask the owner / vault |
| LLM_API_KEY | Model provider key | **yes** | ask the owner / vault |
> Secrets: you will be asked for these — they are not stored in the repo.

## 3. Backing services
<Option A — local via compose:>
```
docker compose -f infra/docker-compose.dev.yml up -d
```
<Option B — forward from the cluster (generated launcher):>
```
# macOS / Linux
sh docs/onboarding/scripts/port-forward.sh
# Windows
powershell -NoProfile -File docs/onboarding/scripts/port-forward.ps1
```
This forwards: <svc:port list the app needs>.

## 4. Build
```
<verified build command>
```

## 5. Run
```
<verified run command(s) — one per service, in start order>
```

## 6. Verify it is up
```
curl -fsS http://localhost:<port>/<health>    # expect: <response>
```

## Troubleshooting
| Symptom | Fix |
|---|---|
| <the walls you hit, and what cleared them> | … |

---
_generated by run-guide from journey-log.md @ <sha>_
```
